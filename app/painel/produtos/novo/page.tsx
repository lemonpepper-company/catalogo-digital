import { ProdutoFormClient } from "../ProdutoFormClient";

export const metadata = {
  title: "Novo produto — Painel",
};

export default function NovoProdutoPage() {
  return <ProdutoFormClient />;
}
