// Fonte única das cores de marca reaproveitadas no código TypeScript.
// Mantém em sincronia com os tokens CSS em app/globals.css (ex.: --color-accent).

/**
 * Cor de destaque padrão de uma loja nova (Gold Dust).
 * Usada como default no cadastro, como fallback nos mapeadores e como
 * primeira opção da paleta ACCENT_COLOR_OPTIONS. Espelha o token --color-accent.
 */
export const DEFAULT_ACCENT_COLOR = "#C9A96E";
